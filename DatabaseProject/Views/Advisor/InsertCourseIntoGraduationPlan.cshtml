@model DatabaseProject.Models.GradPlanCourse

@{
    ViewBag.Title = "InsertCourseIntoGraduationPlan";
}

<h2>Insert Course to Graduation Plan</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>

}

@using (Html.BeginForm("advisorInsertCourseGradPlan","Advisor",FormMethod.Post))
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">

    <hr />

    <div class="form-group">
        @Html.LabelFor(model => model.gradPlan.student.student_id, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownListFor(model => model.gradPlan.student.student_id, ViewData["studentData"] as SelectList, "Select Student",
                    new { @class = "form-control", @name = "student_id" })
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.semester.semester_code, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownListFor(model => model.semester.semester_code, ViewData["semesterData"] as SelectList, "Select Semester",
                    new { @class = "form-control", @name = "semester_code" })
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.course.name, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownListFor(model =>model.course.name, ViewData["courseData"] as SelectList, "Select Course",
                    new { @class = "form-control", @name = "name" })
        </div>
    </div>




    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Insert Course into Graduation Plan" class="btn btn-default btn-primary" style="margin-top: 10px; margin-bottom: 10px" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to Main Page", "./Index")
</div>
