@using DatabaseProject.Models
@model List<StudentCourseViewModel>

@{
    ViewBag.Title = "ViewStudentsOnMajorResult";
}

<h2>Your Advising Students Based On Major</h2>

<p>
    @Html.ActionLink("Return", "./GetMajor")
</p>
<table class="table">
    <tr>
        <th>@Html.DisplayNameFor(model => model[0].student.student_id)</th>
        <th>@Html.DisplayNameFor(model => model[0].student.f_name)</th>
        <th>@Html.DisplayNameFor(model => model[0].student.major)</th>
        <th>@Html.DisplayNameFor(model => model[0].course.name)</th>

        <th></th>
    </tr>


    </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.student.student_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.student.f_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.student.major)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.course.name)
            </td>
            
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Back to Main Page", "./Index")
</div>